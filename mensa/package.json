{
  "name": "mensa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Gestione degli ordini pasto di una mensa aziendale",
  "scripts": {
    "start": "node dev-server.js",
    "test": "node --test --no-warnings test/api.test.js",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute mensa --remote --file=schema.sql",
    "codice": "node scripts/codice-ristorante.js",
    "demo": "node demo/costruisci.js"
  },
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "sql.js": "1.13.0"
  }
}
