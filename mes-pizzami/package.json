{
  "name": "mes-pizzami",
  "version": "0.1.0",
  "private": true,
  "description": "MES Pizzami - modulo bancali prodotto finito e lettura GS1-128",
  "type": "module",
  "engines": {
    "node": ">=22.9"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server/index.js",
    "dev": "node --watch --env-file-if-exists=.env src/server/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mssql": "^12.0.0"
  }
}
